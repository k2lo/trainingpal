<%= form_with(model: training, local: true) do |form| %>
  <% if training.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(training.errors.count, "error") %> prohibited this training from being saved:</h2>

      <ul>
      <% training.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :cover %>
    <%= form.file_field :cover %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <%= form.fields_for :excercises do |excercise| %>
      <%= render 'excercise_fields', f: excercise %>
  <% end %>
  </div>

  <div class="links">
    <%= link_to_add_association 'add excercise', form, :excercises %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
